# Blockers

Tasks that could not be completed and need a human, plus tasks to skip.
An iteration that lands here must stop rather than guess.

Format:
## <task id> — <short title>
- failed at:
- error:
- tried:
- needs:
